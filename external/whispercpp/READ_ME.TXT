To make GGML and Whisper-cpp compatible with Praat,
we added the file `ggml_praat.h` at the top of
`ggml.c`, `ggml-backend-reg.cpp` and `ggml-cpu.c`,
and the file `whisper_praat,h` at the top of `whisper.cpp`.


